## What this changes

<!-- A sentence or two on what and why. -->

## Checklist

- [ ] `npm run lint` passes
- [ ] `npm run build` passes
- [ ] Ran the relevant script(s) in `testing/` if I changed `src/lib/` or `functions/_lib/`
- [ ] No secrets or `.env` values included
